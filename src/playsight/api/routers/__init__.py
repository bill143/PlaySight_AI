"""Phase-1 API routers, all mounted under ``/api/v1`` by ``playsight.api.main``.

Modules: ``auth``, ``teams``, ``players``, ``matches``, ``jobs``,
``artifacts``, ``publish``, ``health``. Phase 2/3 module routers are
discovered via ``playsight.modules.iter_routers()``.
"""
