[project]
name = "playsight-ai"
version = "1.0.0"
description = "Multi-sport video analytics platform"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
